# Test/dev lock: the runtime lock plus a pinned test toolchain.
# CI installs THIS file so the verification environment is fully determined.
-r requirements.txt

pytest==9.1.0
iniconfig==2.3.0
pluggy==1.6.0
# pytest back-compat shims, only needed below 3.11:
exceptiongroup==1.3.1 ; python_version < "3.11"
tomli==2.4.1 ; python_version < "3.11"
pygments==2.11.2
typing_extensions==4.15.0 ; python_version < "3.11"
